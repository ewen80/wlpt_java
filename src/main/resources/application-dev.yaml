logging:
  level:
    root: INFO

spring:
  output:
    ansi:
      enabled: DETECT

  jpa:
    show-sql: true
    hibernate:
      ddl-auto: update
    properties:
      hibernate:
        format_sql: true

  datasource:
    url: jdbc:mysql://127.0.0.1:3306/changdi?useSSL=false&characterEncoding=utf8
    username: wenliang
    password: wenliang


  #业务自定义配置
bizconfig:
  file:
    # 文件上传路径
    file_upload_root_path: D:\\开发项目\\WLPT\\wlpt_angular_alain\\downloads\\  #以斜杠结尾，表示目录
    # 卫星场地审核单模板
    weixing_field_audit_template: D:\\weixing.pdf

frontendUrls: http://localhost:4200  # 前端程序部署地址